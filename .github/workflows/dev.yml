name: dev

on:
  push:
    branches-ignore:
      - 'main'

jobs:
  setup:
    runs-on: windows-latest
    outputs:
      VERSION: ${{ steps.version_inspect.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: '0'
          
      - name: install versionize
        run: dotnet tool install --global Versionize

      - name: setup git
        run: |
          git config --local user.email "dawid.wozny@zoryatec.com"
          git config --local user.name "github-actions[bot]"

      - name: output version
        id: versionize
        run: |
          $VERSION = versionize inspect
          Write-Host "VERSION=$VERSION"
          echo "::set-output name=version::$VERSION"
        continue-on-error: true

      - name: Use version in next steps
        run: |
          echo "The version is ${{ steps.version_inspect.outputs.version }}"


    
        
  ci-cd:
    uses: ./.github/workflows/ci-cd.yml
    needs: setup
    secrets: inherit
    with:
      version: "0.5.0-${{github.run_number}}"
      name: gcd
      nuget-feed: ""
      nipkg-feed: ${{vars.NIPK_DEV_FEED_READABLE}}
